<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/cl_background_deep">

    <!-- Header -->
    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="16dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Widget Yönetimi"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/cl_text_primary" />

        <ImageButton
            android:id="@+id/btn_close_dialog"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:tint="@color/cl_text_primary"
            android:scaleType="centerInside" />
    </LinearLayout>

    <!-- List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/control_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/cl_surface"
        android:padding="4dp"
        android:scrollbars="vertical" />

    <!-- Layout Settings moved to Main Settings -->

    <!-- Footer Actions -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="16dp"
        android:gravity="end">

        <Button
            android:id="@+id/btn_add_new"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+ Yeni Ekle"
            android:backgroundTint="@color/cl_primary"
            android:textColor="@color/cl_text_primary" />

        <Space
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <Button
            android:id="@+id/btn_save_close"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Kaydet &amp; Kapat"
            android:backgroundTint="@color/cl_success"
            android:textColor="@color/cl_text_primary" />

    </LinearLayout>

</LinearLayout>
