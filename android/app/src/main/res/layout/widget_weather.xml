<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_modern_card">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="12dp"
        android:gravity="center">

        <!-- Konum Etiketi -->
        <TextView
            android:id="@+id/weather_location"
            style="@style/CarWidget.Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="KONUM"
            android:textColor="@color/cl_text_hint"
            android:textSize="10sp"
            android:layout_marginBottom="2dp"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/weather_icon"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:src="@drawable/ic_action_umbrella"
                app:tint="@color/cl_accent_gold"
                android:scaleType="fitCenter" />

            <TextView
                android:id="@+id/weather_temp"
                style="@style/CarWidget.Value"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="--°"
                android:textSize="36sp"
                android:layout_marginStart="8dp"
                android:includeFontPadding="false"/>

        </LinearLayout>

        <!-- Durum Aciklamasi -->
        <TextView
            android:id="@+id/weather_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Hava Durumu"
            android:textColor="@color/cl_text_secondary"
            android:textSize="14sp"
            android:singleLine="true"
            android:layout_marginTop="4dp"
            android:fontFamily="sans-serif-medium"/>

    </LinearLayout>
    
    <!-- Loading -->
    <ProgressBar
        android:id="@+id/weather_loading"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="top|end"
        android:layout_margin="8dp"
        android:visibility="gone"
        android:indeterminateTint="@color/cl_accent_gold"/>

</FrameLayout>
